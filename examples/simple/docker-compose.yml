version: "3.8"
services:
  web:
    image: nginx:1.25
    ports:
      - "8080:80"
    environment:
      LOG_LEVEL: info
      UPSTREAM: http://redis:6379
  redis:
    image: redis:7
    ports:
      - "6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data: {}
